{
  "buildCommand": "cd frontend && npm install && VITE_API_URL=https://highrise-backend-production.up.railway.app/api npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
